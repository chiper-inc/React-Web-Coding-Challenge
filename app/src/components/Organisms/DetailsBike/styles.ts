import { globalStyles } from '@/styles/globalStyles';
import styled from 'styled-components';

export const Container = styled.div`
  margin: ${globalStyles.metrics.margin};
`;

export const Map = styled.div`
  width: 100%;
  max-height: 500px;
`;
