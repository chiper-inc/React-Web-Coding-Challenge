import styled from 'styled-components';

export const HeaderContent = styled.header`
  margin: 2rem;
  
  .title {
    font-size: 3rem;
    font-weight: bold;
  }
  
  .subtitle {
    font-size: 2rem;
  }
`;
